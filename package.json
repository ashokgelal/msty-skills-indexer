{
  "name": "msty-skills-indexer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Build a static Skills.sh catalog index and publish it to Cloudflare R2.",
  "scripts": {
    "scrape": "node scripts/scrape.mjs",
    "scrape:dry": "DRY_RUN=1 node scripts/scrape.mjs",
    "lint": "node --check scripts/scrape.mjs"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.812.0",
    "cheerio": "^1.0.0",
    "p-limit": "^6.2.0"
  },
  "engines": {
    "node": ">=20"
  }
}
